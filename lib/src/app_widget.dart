import 'package:flutter/material.dart';

import 'features/onboard_page.dart';

class AppWidget extends StatefulWidget {
  const AppWidget({Key? key}) : super(key: key);

  @override
  State<AppWidget> createState() => OnboardPage();
}